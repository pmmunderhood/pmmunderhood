name: Deploy to pmmunderhood.github.io

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
      with:
        path: project

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Run build on npm setup
      working-directory: ./project
      run: |
        npm ci
        npm run build
        
    - name: Checkout ghpages repo
      uses: actions/checkout@v2
      with:
        repository: pmmunderhood/pmmunderhood.github.io
        path: github.io
        token: ${{ secrets.CUSTOM_GITHUB_TOKEN }}

    - name: Remove all files from repo
      run: git rm -r .
      working-directory: ./github.io
    
    - name: Copy dist to github.io repo
      run: cp -a project/dist/. github.io/

    - name: Commit and Push to pmmunderhood.github.io
      working-directory: ./github.io # Make failable
      run: | 
        git add .
        git config --local user.email "ci@github"
        git config --local user.name "pmmunderhood"
        (git commit -m "Deploy to github.io $(date)"; git push https://$REPO_OWNER:$CUSTOM_GITHUB_TOKEN@github.com/pmmunderhood/pmmunderhood.github.io) || echo "Nothing to commit"
      env:
        REPO_OWNER: ${{ secrets.repository_owner }}
        CUSTOM_GITHUB_TOKEN: ${{ secrets.CUSTOM_GITHUB_TOKEN }}
